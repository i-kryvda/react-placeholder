# react query + json server (CRUD)

- CRUD [GET, PATCH, DELETE, POST]
- react query
- json server

---

- UI [ProgressBar]
- api sort, [prev, next], total
- load more with react-query
- pagination [prev, next] UI logic
- ? Є можливість робити pagination [prev, next] за допомогою useInfiniteQuery (це на майбутнє)
