# Redux createAsyncThunk

> Робимо запит на сервер за допомогою createAsyncThunk,
> Обрабляємо pending, fulfilled, rejected у extraReducers
> Виводимо повідовлення error за допомогою rejectWithValue
> Робимо createAction та передаємо його у extraReducers для counter
> Мінімум Boilerplate

- createAsyncThunk, extraReducers, rejectWithValue, createAction
